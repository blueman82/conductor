max_concurrency: 7
timeout: 45m
log_level: debug
log_dir: /tmp/conductor/logs
dry_run: true
skip_completed: true
retry_failed: true

learning:
  enabled: true
  db_path: /tmp/learning.db
  swap_during_retries: false
  qc_reads_plan_context: false
  qc_reads_db_context: false
  max_context_entries: 25
  keep_executions_days: 45
  max_executions_per_task: 150

quality_control:
  enabled: true
  review_agent: custom-qc
  retry_on_red: 3
  agents:
    mode: explicit
    explicit_list:
      - qc-a
      - qc-b
    additional:
      - extra-agent
    blocked:
      - blocked-agent
